# 环境变量模板 — 复制为 .env 后修改
# cp .env.example .env

# Flask 密钥（生产环境必须修改为随机字符串）
SECRET_KEY=your-secret-key-change-me

# MySQL 配置（密码避免使用 @、:、/ 等 URL 特殊字符，或在 DATABASE_URL 中做 URL 编码）
MYSQL_ROOT_PASSWORD=RebarMgmt2026
MYSQL_DATABASE=rebar_mgmt
MYSQL_USER=rebar
MYSQL_PASSWORD=RebarMgmt2026
