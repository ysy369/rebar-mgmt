# 开发日志 — 2026-06-23

## 今日完成
- [x] 增强 Flask `/health` 端点：新增数据库连接检查、上传/导出目录可写检查，异常时返回 HTTP 503
- [x] 更新 `docker-compose.yml`：
  - 保持 `restart: unless-stopped` 覆盖容器崩溃自动重启
  - 新增 `autoheal` 服务，对 healthcheck 失败（unhealthy）的 `rebar-app` 自动重启
  - 给 `rebar-app` 添加 `autoheal=true` 标签
- [x] 新增 Linux 宿主机看门狗与开机自启方案：
  - `scripts/rebar-startup.sh`：一键启动/重启 Docker Compose 平台
  - `scripts/rebar-health-watchdog.sh`：每 30 秒检查 `/health` 与容器健康状态，写入 `status/rebar-health.json`，异常时生成 `status/rebar-unhealthy.marker`
  - `scripts/systemd/rebar-mgmt.service`：服务器开机自动启动平台
  - `scripts/systemd/rebar-health-watchdog.service` / `.timer`：定时触发看门狗
  - `scripts/systemd/install.sh`：一键安装并启用 systemd 单元
- [x] 新增 `status/`、`logs/` 目录，用于存放健康状态标记与日志
- [x] 新增 `docs/09-健康检查与自动恢复.md` 运维文档
- [x] 更新 `CLAUDE.md` 文档索引

## 遇到的问题
- 用户明确后续部署在 Ubuntu 云服务器，因此本次不添加 Windows 计划任务，专注 Linux/systemd 方案。
- 看门狗脚本中 curl 调用需兼容容器未启动或网络不通的情况，已使用 `--max-time` 与 `|| echo 0` 处理。

## 明日计划
- [ ] 按文档验证 `/health` 与 `autoheal` 在实际 Ubuntu 环境中的表现
- [ ] 验证 systemd 开机自启与看门狗定时器
- [ ] 如有需要，补充应用端日志文件输出配置

## 备注
- `autoheal` 需要访问 Docker Socket，生产服务器需确保 Docker 用户权限正确。
- 当前看门狗默认不主动重启容器（由 `autoheal` 负责），可通过 `RESTART_UNHEALTHY=1` 开启第二重保护。
