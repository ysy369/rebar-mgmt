# 开发日志 — 2026-06-24

## 今日完成

- [x] 重写 `app/templates/base.html` 侧边栏菜单，按新蓝图结构组织：首页 / 项目管理 / 劳务分包管理 / 审核与分析 / 现场管理 / 工作管理 / 系统设置。
- [x] 新增 6 个蓝图路由文件并注册：
  - `app/routes/home.py`（/home：KPI + 筛选 + 工作台）
  - `app/routes/labor.py`（/labor：浅色风格 4 模块）
  - `app/routes/audit.py`（/audit/*：盈亏/数据动态/采购计划/对比测算）
  - `app/routes/site.py`（/site/*：形象进度/物资领用/巡检/代换/优化台账）
  - `app/routes/work.py`（/work：3 Tab 工作管理）
  - `app/routes/system.py`（/system/users：复用 admin 用户 CRUD）
- [x] 在 `app/__init__.py` 注册上述 6 个新蓝图。
- [x] 扩展 `app/models/project.py`：
  - `status` 从 `Enum('active','archived')` 改为 `VARCHAR(20)`，支持 4 种状态：`finished`/`in_progress`/`not_started`/`stopped`；
  - 新增 `contract_amount`（合同额万元）和 `output_value`（累计产值万元）。
- [x] 更新 `app/__init__.py` 的 `_auto_migrate`：
  - 自动添加 `contract_amount`、`output_value` 列；
  - 自动将旧 `active`/`archived` 映射为 `in_progress`/`finished`，并将 `status` 列类型改为 `VARCHAR(20)`；
  - 自动创建 `imported_files` 表。
- [x] 创建 `app/models/imported_file.py` 并注册到 `app/models/__init__.py`。
- [x] 调整 `app/routes/project.py`：
  - `/project/dashboard` 改为项目数据看板；
  - 旧项目列表迁到 `/project/list`；
  - 新增 8 个表单路由：model-sheet / entry-sheet / transfer-sheet / waste-sheet / remaining-sheet / secondary-sheet / detailing-sheet / non-budget-sheet；
  - 新增 `/project/import` 文件导入页，上传 Excel 保存到 `imported_files` 表；
  - 新增 `/project/imported-files/<id>/download` 与 `/project/imported-files/<id>/delete`。
- [x] 8 个项目管理表单页统一使用 `templates/project/sheet_base.html` 渲染为文件列表（搜索框 + 表格 + 上传入口）。
- [x] 创建 `templates/project/import.html` 导入页。
- [x] 重写 `templates/home/index.html`：
  - 删除顶部标题卡片，筛选项直接贴顶；
  - 新增 5 张深色 KPI 卡片：项目数量 / 产值万元 / 合同额万元 / 已完工数 / 在建数；
  - 保留 4 张白色指标卡片：预算总量 / 消耗总量 / 翻样总量 / 进场总量；
  - 筛选新增统计周期 / 承接公司 / 甲方单位 / 项目多选 / 项目状态；
  - 「业务模块」改名为「工作台」，保留 8 入口。
- [x] 重写 `templates/labor/index.html` 为浅色风格（#f5f6fa 白卡片）：
  - 上排 50/50：单体明细表（9 列）/ 盈亏罚扣表（5 列）；
  - 下排 50/50：分包总结算量（大字青色/橙色）/ 分包报量预警（红色角标）；
  - 底部蓝色说明文字区域。
- [x] 调整 `templates/home/index.html` 筛选项位置：从顶部下移到 4 张白色卡片与工作台之间。
- [x] 更新 `app/models/imported_file.py`：
  - `ledger_type` 枚举扩展 3 个新值：`secondary`（钢筋二构表）、`detailing`（钢筋翻样表）、`non_budget`（非预算收入使用钢筋表）；
  - 保留 `measure_rebar` / `fangyang_requisition` / `progress` 用于文件管理模块。
- [x] 更新 `app/__init__.py` 的 `_auto_migrate`：
  - 新建 `imported_files` 表时使用包含 11 个值的枚举；
  - 已存在表时自动 `ALTER TABLE` 扩展 `ledger_type` 枚举。
- [x] 更新 `app/routes/project.py`：
  - `LEDGER_TYPE_LABELS` 与 `SHEET_LEDGER_MAP` 增加 3 个新类型映射；
  - `_render_sheet` 统一从 `LEDGER_TYPE_LABELS` 读取中文标题。
- [x] 更新 `templates/project/import.html`：台账类型改为下拉框，包含 8 个与项目管理菜单对应的选项。
- [x] 更新 `templates/base.html`：去掉劳务分包管理的 `target="_blank"`，改为普通跳转。

## 遇到的问题

- 计划模式提交时被用户中断，随后用户直接给出更详细的菜单/路由/页面规格，按该规格继续实现。
- 运行 `python -m py_compile` 进行语法检查时，Bash 工具因模型临时不可用未能执行，需稍后补做启动验证。

## 明日计划

- [ ] 运行应用并逐个验证新路由可访问（/home、/labor、/audit/*、/site/*、/work、/system/users、/project/dashboard、/project/list、/project/*-sheet、/project/import）。
- [ ] 检查侧边栏菜单高亮与折叠是否正常，尤其三级菜单（钢筋对比测算）。
- [ ] 测试文件上传、下载、删除流程，验证新 ledger_type 能否正常保存。

## 备注

- `/system/users` 完整复用了 `User` 模型与 admin 用户管理的增删改查逻辑，模板独立但结构与 `admin/users/list.html` 保持一致。
- 深色大屏公共骨架 `templates/components/dark_dashboard_base.html` 已保留，后续 `/audit/profit-loss`、`/site/visual-progress` 等大屏页可快速复用。
- `Project.status` 已完成从旧枚举到新 4 种状态的自动迁移逻辑，但需启动应用后由 `_auto_migrate` 实际执行；旧数据 `active` → `in_progress`，`archived` → `finished`。
- `imported_files.ledger_type` 扩展新枚举后，已存在的历史数据无需迁移（旧值仍有效，新值仅后续上传使用）。
